# from .interfaces import IFormRendererSet
# from zope.interface import implements

# def add_formrender(config, renderer=None, display_fields=None):
#     frset = config.registry.queryUtility(IFormRendererSet)
#     if frset is None:
#         frset = FormRendererSet()
#         config.registry.registerUtility(frset, IFormRendererSet)
    


